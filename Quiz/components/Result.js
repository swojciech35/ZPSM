import * as React from 'react';
import { View, Text } from 'react-native';


function Result() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', }}>
        <Text style={{color:'black'}}>Result Screen</Text>
      </View>
    );
  }

export default Result;